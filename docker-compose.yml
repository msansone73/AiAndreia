services:

  # ── AiAndreia Application ──────────────────────────────────
  app:
    build: .
    container_name: aiandreia-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      # Override application.properties via Spring relaxed binding
      SPRING_DATASOURCE_URL: jdbc:postgresql://5.189.153.30:5432/sansone-db
      SPRING_DATASOURCE_USERNAME: msansone
      SPRING_DATASOURCE_PASSWORD: senha
      OLLAMA_API_URL: http://5.189.153.30:11434

